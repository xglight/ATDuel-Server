# 竞赛评分系统设计文档

本文档详细描述了本编程竞赛平台使用的评分更新算法。该系统基于 **ELO 等级分系统** 进行改良，针对团队对抗、题目数量少 ($N \in [1,10]$) 以及人数不平衡 ($1\text{-}3$ 人) 的场景进行了专门优化。

## 1. 核心设计理念

本系统旨在实现以下公平性目标：

1.  **团队实力的科学评估**：不仅看平均分，还考虑人数带来的并发解题优势。
2.  **公平的奖惩机制**：
    *   **赢了**：依据贡献计算 Rating，兼顾团队协作保底。
    *   **输了**：**能力越大，责任越大**。
3.  **精确的贡献计算**：基于得分差计算表现分。
4.  **快速定级**：新选手前 5 场比赛拥有更高的变动系数，以快速定位段位。

---

## 3. 数学模型详解

### 3.1 团队有效评分 (Effective Team Rating)

设队伍成员集合为 $U$，人数为 $k$，成员个人评分为 $R_i$。

$$ R_{team} = \frac{\sum R_i}{k} + (k - 1) \times 50 $$

*   **基础战力**：取成员平均分。
*   **人数补正**：每多一名队友，团队有效评分增加 50 分。

### 3.2 期望胜率 (Expectation)

基于标准 ELO 逻辑，计算 A 队对战 B 队的期望胜率 $E_A$：

$$ E_A = \frac{1}{1 + 10^{(R_{B,eff} - R_{A,eff}) / 400}} $$

*   $E_A \in (0, 1)$。

### 3.3 实际表现分 (Actual Performance)

设比赛题目数为 $n$，第 $i$ 题分值为 $100 \times i$。
总分池 $S_{total} = 100 \times \frac{n(n+1)}{2}$。

$$ S_{actual} = 0.5 + 0.5 \times \frac{Score_A - Score_B}{S_{total}} $$

### 3.4 团队变动总池 (Delta Pool)

计算整个队伍应当获得或扣除的总分。

$$ \Delta_{Team} = K \times (S_{actual} - E_A) \times k $$

*   **K 因子 ($K$)**：标准比赛 $K=32$。
*   **乘 $k$**：将 1v1 的变动量放大到团队规模，作为总分配池。

---

## 4. 个人分数分配策略 (核心机制)

这是本系统与传统 ELO 最大的不同点。我们将 $\Delta_{Team}$ 分配给个人时，采用**非对称分配**。

### 4.1 当队伍获胜 / 表现优于预期 ($\Delta_{Team} > 0$)

**策略：基础奖励 + 贡献奖励**

对于选手 $u$，其分配系数 $W_u$ 为：
$$ W_u = \underbrace{0.3 \times \frac{1}{k}}_{\text{团队协作奖}} + \underbrace{0.7 \times \frac{Score_u}{Score_{Team}}}_{\text{MVP 贡献奖}} $$

$$ \Delta_u = \Delta_{Team} \times W_u $$

### 4.2 当队伍失败 / 表现低于预期 ($\Delta_{Team} < 0$)

**策略：责任权重制 (Responsibility Weighted)**

对于选手 $u$，其分配系数 $W_u$ 为：
$$ W_u = \frac{R_u}{\sum_{i \in Team} R_i} $$

$$ \Delta_u = \Delta_{Team} \times W_u $$

---

## 5. 特殊规则

### 5.1 定级赛机制 (Placement Matches)
*   **定义**：选手参赛场次 $< 5$ 场。
*   **效果**：最终变动值 $\Delta_{final} = \Delta_u \times 2.5$。

### 5.2 最低分保护
*   计算完成后，若 Rating $< 0$，强制修正为 0。

### 5.3 极端情况处理 ($N=1$)
*   当只有 1 道题时，胜者直接获得全部贡献度 ($100\%$)，败者得分为 0。
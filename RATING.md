# 竞赛平台评分系统设计文档

## 1. 概述与设计目标

本系统基于 ELO 等级分算法改良，专为 **1-3人团队**、**少题目数 ($N \in [1,10]$)** 的编程对抗赛设计。

### 核心设计原则
1.  **方向绝对性**：获胜方 Rating **必须增加**，失败方 Rating **必须减少**（杜绝“赢了比赛输了分”的情况）。
2.  **团队修正**：认可人数优势，3 人队的有效战力高于同分数的 1 人队。
3.  **奖惩分配**：
    *   **胜方**：兼顾团队保底与 MVP 激励（能者多劳，多劳多得）。
    *   **败方**：实施“高分高责”制（大佬带队输了要多扣，萌新少扣）。
4.  **抗通胀与保底**：设置最小获胜奖励，防止强队打弱队无收益；设置零和博弈机制控制分值膨胀。

---

## 2. 基础定义与参数

| 符号 | 定义 | 推荐值/说明 |
| :--- | :--- | :--- |
| $R_u$ | 选手个人 Rating | 初始分 1500，下限 0 |
| $k$ | 队伍人数 | $1 \le k \le 3$ |
| $K$ | 基础变动系数 (K-Factor) | **32** (标准赛事) |
| $\Delta_{min}$ | 胜场最低奖励分 | **5** (防止强胜弱 0 收益) |
| $S_{total}$ | 比赛总分池 | 所有题目分数之和 |

---

## 3. 团队对抗数学模型

### 3.1 团队有效评分 (Effective Team Rating)

单纯累加或平均无法反映人数带来的“并发解题”优势。我们需要给多人队进行战力补偿。
设队伍成员集合为 $U$，人数为 $k$：

$$ R_{team} = \frac{\sum_{i \in U} R_i}{k} + (k - 1) \times 50 $$

*   *解读*：基础分为成员平均分。每多一名队友，团队有效战力 $+50$。

### 3.2 期望胜率 (Expectation)

基于 Logistic 曲线计算 A 队对战 B 队的胜率期望 $E_A$：

$$ E_A = \frac{1}{1 + 10^{(R_{B,eff} - R_{A,eff}) / 400}} $$

*   $E_A \in (0, 1)$。
*   若 A 队评分远高于 B 队，则 $E_A \to 1$。

### 3.3 统治力系数 (Dominance Factor)

为了量化“赢”的程度，引入统治力系数 $\alpha$。
设 $S_{win}$ 为胜方得分，$S_{lose}$ 为败方得分，$S_{total}$ 为本场题目总分。

$$ \alpha = 0.5 + 0.5 \times \frac{S_{win} - S_{lose}}{S_{total}} $$

*   **险胜** ($S_{win} \approx S_{lose}$)：$\alpha \to 0.5$。赢得艰难，加分打折。
*   **完胜** ($S_{lose} = 0$)：$\alpha \to 1.0$。赢得漂亮，满额加分。

### 3.4 团队变动总池计算 (Delta Calculation)

此步骤确立“赢涨输跌”的绝对规则。

#### 第一步：计算基础变动量
根据 ELO 核心思想，弱胜强奖励大，强胜弱奖励小。
$$ \Delta_{raw} = K \times (1 - E_{winner}) $$

#### 第二步：应用保底与统治力修正 (胜方总池)
$$ \Delta_{TeamWin} = k \times \max(\Delta_{raw}, \Delta_{min}) \times \alpha $$

*   **$\max(\dots, \Delta_{min})$**：确保即使是世界冠军打败新手，也能获得最低限度的辛苦分（例如 5 分）。
*   **$\times k$**：将分数放大到团队规模。
*   **$\times \alpha$**：根据赢得是否漂亮进行最终缩放。

#### 第三步：确定败方总池
$$ \Delta_{TeamLose} = -1 \times \Delta_{TeamWin} $$

---

## 4. 个人分数分配策略

将团队的总变动分 $\Delta_{Team}$ 分配给每个队员。

### 4.1 胜方分配策略：协作 + 贡献

**逻辑**：赢了大家都有份（协作奖），谁写出来的题多谁拿大头（MVP 奖）。
设队伍总分为 $Score_{Team}$，个人得分为 $Score_u$。

**分配系数 $W_u$：**
$$ W_u = \underbrace{0.4 \times \frac{1}{k}}_{\text{团队协作(40\%)}} + \underbrace{0.6 \times \frac{Score_u}{Score_{Team}}}_{\text{MVP贡献(60\%)}} $$

**个人变动：**
$$ \Delta_u = \Delta_{TeamWin} \times W_u $$

*   *特殊情况*：若对手弃权导致双方得分为 0 但判胜，则 $Score_u/Score_{Team}$ 按 $1/k$ 计算。

### 4.2 败方分配策略：能力责任制

**逻辑**：输了比赛，高分选手（老手）承担更多责任，低分选手（新手）受保护。

**分配系数 $W_u$：**
$$ W_u = \frac{R_u}{\sum_{i \in Team} R_i} $$

**个人变动：**
$$ \Delta_u = \Delta_{TeamLose} \times W_u $$

---

## 5. 特殊规则与边界处理

### 5.1 定级赛机制 (Placement Matches)
*   **条件**：选手历史参赛场次 $\le 5$。
*   **效果**：该选手的最终变动值 $\Delta_{final} = \Delta_u \times 2.5$。
*   **目的**：让炸鱼的小号快速升段，让新手快速匹配到同水平对手。

### 5.2 最低分保护
*   任何计算结束后，若 Rating $< 0$，强制修正为 0。

### 5.3 1v1 极简情况
*   当 $k=1$ 时，上述公式自然退化为标准 1v1 模式，依然适用。